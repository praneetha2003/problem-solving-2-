#include <stdio.h>

int main()
{
    int i=0,number;
    printf("enter the number\n");
    scanf("%d",&number);
    for(i;i<=10;i++)
    {
        printf("the multiplication table is %d*%d=%d\n",i,number,i*number);
    }

    return 0;
}
